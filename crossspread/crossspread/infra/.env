# CrossSpread Environment Configuration
# Copy this to .env and customize

# App
APP_ENV=development
APP_PORT=8000
TZ=UTC+2

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=crossspread
DB_PASS=changeme
DB_NAME=crossspread

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Encryption (base64 encoded 32-byte key for AES-256)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY_BASE64=dGhpc2lzYXZlcnlzZWN1cmVrZXlmb3J0ZXN0aW5nMTIz

# Execution microservice
EXEC_SERVICE_HOST=localhost
EXEC_SERVICE_PORT=9000

# JWT / Auth
JWT_SECRET=supersecretjwtkey-change-in-production
JWT_EXPIRY=86400

# Grafana (observability profile)
GRAFANA_PASSWORD=admin

# Market Data Ingest
ENABLED_EXCHANGES=binance,bybit,okx,kucoin
LOG_LEVEL=info
METRICS_PORT=9090
