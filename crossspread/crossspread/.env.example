# App
APP_ENV=development
APP_PORT=8000
TZ=Europe/Kyiv

# Postgres
DB_HOST=postgres
DB_PORT=5432
DB_USER=crossspread
DB_PASS=changeme
DB_NAME=crossspread

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Encryption (for MVP, a base64 32 byte key; use KMS in prod)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY_BASE64=CHANGE_THIS_BASE64_32_BYTES_KEY_HERE

# Execution microservice
EXEC_SERVICE_HOST=execution
EXEC_SERVICE_PORT=9000

# JWT / Auth
JWT_SECRET=supersecretjwtkey_change_in_production
JWT_EXPIRY=86400

# Market Data Ingest
MD_INGEST_HOST=md-ingest
MD_INGEST_PORT=8080

# Frontend
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# Prometheus / Grafana (optional)
PROM_PUSHGATEWAY=prometheus:9091

# Exchange API Keys (per-user, encrypted in DB)
# These are only for testing - real keys stored encrypted per-user
# BINANCE_API_KEY=
# BINANCE_API_SECRET=
# BYBIT_API_KEY=
# BYBIT_API_SECRET=
